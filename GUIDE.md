# DataMove 新手引导与使用说明

> 目标读者: **第一次登录本系统的操作人员**。不需要懂数据库底层原理, 跟着做就能用。
>
> 部署安装请看 [QUICKSTART.md](QUICKSTART.md), 完整功能/参数说明请看 [README.md](README.md)。

---

## 一、这是干什么的工具?(30 秒版)

**把一个 MySQL 库里的表, 自动搬到另一个 MySQL 库里, 搬完还能自动对账。**

典型用途:

- 上新系统, 老库数据要迁到新库
- 生产库 → 报表库 / 归档库 / 测试库
- 两个系统之间要共享部分数据

你只需要在网页上: **填两个数据库地址 → 建一个任务 → 点启动**。剩下的事(分批读取、断点续传、失败告警、数据对账)系统全包。

---

## 二、页面地图: 哪个菜单是干什么用的?

登录后左侧菜单一共 9 个页面, **新手只需要先认识前 4 个**(带 ★ 的):

| 菜单 | 干什么 | 新手什么时候用 |
|------|--------|----------------|
| ★ 数据源管理 | 登记"要同步的数据库"的地址账号 | **第 1 步必用** |
| ★ 同步任务 | 创建/启动/暂停任务, 一切配置都在这 | **第 2 步必用, 最常用** |
| ★ 任务大盘 | 实时看任务跑多快、还剩多久 | 任务跑起来后看 |
| ★ 同步日志 | 每一批搬了哪些行、有没有报错 | 任务失败时排查 |
| 数据中心 | 像 Excel 一样浏览/编辑任意表数据、改表结构 | 平时查数据用 |
| SQL 工作台 | 网页里直接写 SQL 执行(带高亮/补全) | 会 SQL 的人用 |
| SQL 执行日志 | 谁在工作台执行过什么 SQL | 留痕审计用 |
| 审计日志 | 谁改了哪个任务的哪个字段 | 管理员追溯变更用 |
| 授权管理 | License 授权状态 | 一般不用动 |

> 记不住就背一句话: **数据源管"库", 任务管"搬", 大盘看"现在", 日志看"过程"。**

---

## 三、新手五步走(第一次使用照着做)

### 第 1 步: 登录并改密码

浏览器打开系统地址(开发环境一般是 `http://localhost:80`), 用管理员给你的账号登录。
admin 首次登录会被强制改密码, **改完记牢**。

### 第 2 步: 登记数据源(要同步的"源库"和"目标库")

进入 **数据源管理** → 点「新增」, 填写:

| 字段 | 填什么 | 例子 |
|------|--------|------|
| 名称 | 随便起, 自己认得就行 | `订单库-生产` |
| IP | 数据库机器地址 | `192.168.1.100` |
| 端口 | MySQL 端口 | `3306` |
| 数据库 | 库名 | `order_db` |
| 账号 / 密码 | 有读写权限的数据库账号 | `root / ******` |

**源库**(数据从哪来)和**目标库**(数据搬到哪去)各登记一条。

> 填完先点「测试连接」, 提示成功再保存 —— 90% 的"任务启动失败"都是数据源连不上。
> 密码保存后是加密的, 页面上只会显示 `ab****ef` 这样的掩码, 不是丢了。

### 第 3 步: 用向导创建第一个任务 (4 步)

进入 **同步任务** → 点右上角蓝色的「**新建任务**」, 会打开一个**四步向导**, 每步只问一个问题:

```
① 选源 → ② 选目标 → ③ 选模式 → ④ 字段映射 → 完成
```

**① 选源** — 数据从哪来

- **源数据源**: 下拉选第 2 步登记过的库
- **同步表名**: 从下拉里选(或直接手输)。选好后下方会绿条提示"读到源表 xxx, 共 N 个字段" —— 没提示说明库连不上, 回第 2 步检查

**② 选目标** — 数据搬到哪去

- **目标数据源**: 下拉选(源库不会出现在列表里 —— 同库同名等于原地同步, 没意义)
- 目标表名固定与源表同名, 这里只显示确认
- 会自动检查目标库有没有这张表: **没有** → 黄条提示先建一次「同步表结构」

**③ 选模式** — 怎么搬、什么时候搬

- **任务名称**: 留空会自动生成 `表名同步`
- **同步方式**(三张卡片, 点一下就选):
  - **全量同步** → 把整张表搬一遍。**第一次迁移选这个**
  - **增量同步** → 源库一有增删改就跟着搬(需要 Canal, 新手先跳过, 见[场景 3](#场景-3实时增量源库一有变化立刻同步过去))
  - **同步表结构** → 只在目标库建表, 不搬数据
- **追数方式**(全量才有): **按主键 ID**(推荐, 最快) / **按更新时间**(只搬这段时间后改过的)
- **什么时候跑**: 「我点按钮才跑」/「定时自动跑」/「外部系统通知」 —— 详见[第四节](#四、调度方式怎么选手动--定时--事件触发), 新手先选「我点按钮才跑」
- 底部的「**高级设置**」默认折叠: 批次大小 / 并行分片数 / 覆盖数据 / 忽略字段 / 告警。**不了解就别动**, 用默认值即可

**④ 字段映射** — 列名对不上时才配

- 左右两栏分别是源表和目标表的字段
- **什么都不连 = 按同名同步全部字段**(大多数情况这样就够了)
- 列名不一致时: 按住左边字段**拖到**右边对应字段上连线; 连线上的 × 可删除
- 同名列很多时点「**自动配对同名字段**」一键连好, 再手动调整个别不同的

点「**完成创建**」, 系统会问要不要立即启动 —— 选「立即启动」就直接开跑。

### 第 4 步: 启动任务

任务列表里找到刚建的任务 → 点「启动」。

**任务状态流转**:

```
已停止 ──启动──▶ 运行中 ──完成──▶ 已停止
                 │
                 ├─暂停──▶ 已暂停 ──继续──▶ 运行中
                 └─终止──▶ 已停止(保留断点, 再启动从断点继续)
```

**如果失败**: 去「同步日志」页, 按状态筛「失败」, 看异常信息列。最常见三种:
1. `Connection refused` → 数据源 IP/端口/账号密码不对
2. `Table doesn't exist` → 目标库没建表(可先建一个 DDL 任务搬表结构)
3. `Duplicate entry` → 目标表已有数据冲突, 确认是否该勾「覆盖数据」

### 第 5 步: 看结果、做对账

1. **任务大盘**: 任务运行中实时看 速率(行/秒)、ETA(还剩多久)、瓶颈在哪边
2. **同步日志**: 每一批的明细, 支持导出 CSV
3. **数据校验**(强烈建议): 任务列表行内点「数据校验」→「开始校验」
   - 系统逐行对比源库和目标库, 告诉你**哪一行、哪个字段**对不上
   - 有差异点「一键同步差异」即可自动补齐修复
   - 放心: 修复**只补缺失、修不一致, 绝不会删目标库的数据**

到这里, 一次完整的同步就跑通了。🎉

---

## 四、调度方式怎么选?(手动 / 定时 / 事件触发)

创建任务时向导第 3 步的「什么时候跑」三选一(编辑任务里叫「调度方式」):

| 调度方式 | 适合场景 | 怎么配 |
|----------|---------|--------|
| **手动 (MANUAL)** | 一次性迁移、临时导数据、想自己控制每次执行 | 不用配, 保存后界面上点「启动」才跑 |
| **定时 (CRON)** | 每晚 2 点跑一次、每小时同步一次等**周期性**任务 | 填 CRON 表达式, 不熟悉表达式就用**可视化 Cron 生成器**(秒/分/时/天/月/周分页签点选)。**保存即生效**, 到点自动跑, 不用再点启动 |
| **事件触发 (EVENT)** | 上游系统跑完了再通知同步开始(如 ETL 完成后回调) | 保存后系统自动生成一个专属 URL, 把它配到外部系统, 事情完成后外部系统 `POST` 一下这个 URL, 任务就被拉起 |

**新手常见疑问**:

- 定时任务**正在跑**时到了下一个触发点怎么办? → 自动**跳过本次**, 不会重复跑
- 事件触发的 URL 是什么样子? → `POST http://你的服务器:8080/sync/task/event/一串令牌`, 令牌即密码, **不要泄露**; 泄露了在任务编辑里重新生成即可
- 三种方式可以随时切换吗? → 可以, 编辑任务改完保存立即生效

---

## 五、场景手册(按需查阅)

### 场景 1: 一次性全量迁移(最常用)

> 老系统的 20 张表搬到新库, 搬完就完事

1. 数据源管理登记两个库, 都「测试连接」
2. 点「新建任务」走向导, 每张表建一个 **全量同步 + 按主键 ID** 任务(只有表结构差异大时才需要在第 4 步配字段映射)
3. 大表(百万行以上)在向导第 3 步展开「高级设置」, 把**并行分片数**调到 4~8, 快很多
4. 创建完向导会问要不要立即启动; 也可回列表逐个启动, 大盘看进度
5. 全部跑完, 每个任务点「数据校验」对账, 有差异一键修复

### 场景 2: 每天定时同步

> 生产库报表数据, 每晚 2 点同步到报表库

1. 按场景 1 建任务
2. 「调度方式」选**定时**, 打开 Cron 生成器 → 天 页签 → 周期执行 → 每天 02:00:00 → 确定
3. 保存。之后不用管, 每晚自动跑, 失败会推钉钉/邮件(记得配告警)
4. 偶尔去「任务大盘」的**运行历史**看看趋势图, 有没有红色失败柱

### 场景 3: 实时增量(源库一有变化立刻同步过去)

> 双库要近乎实时一致, 如读写分离、异地备份

**前置条件**(管理员做一次):
1. 源库开启 binlog ROW 模式(`SET GLOBAL binlog_format = 'ROW';`)
2. 部署 Canal Server 并指向源库(详见 README 第九节)

**配置**:
1. 建任务选 **增量 (INCR)**, 填 Canal Host / Port(默认 11111)/ Destination
2. 需要过滤时用 **DML 过滤**(如归档库只勾 INSERT)和**忽略字段**(如目标库自维护的 `update_by`)
3. 启动后任务常驻运行, 新增/修改/删除会实时同步过去
4. binlog 断开自动告警, 重启任务会从**上次位点**继续, 不丢数据

### 场景 4: 外部系统跑完再触发同步

> 数据仓库凌晨 1 点跑完 ETL, 跑完才允许 DataMove 同步

1. 建任务, 调度方式选**事件触发**, 保存
2. 复制系统生成的回调 URL
3. 在外部系统的 ETL 末尾加一步: `curl -X POST <回调URL>`
4. 每次外部系统调用, 任务即被拉起; 任务还在跑时的重复调用会被自动忽略

### 场景 5: 同步完对账 + 一键修复

> 怎么确认两边数据一模一样?

1. 任务列表行内点「数据校验」→ 先看最近一次结果, 需要重跑点「开始校验」
2. 校验是逐行比对的: **缺失**(目标少行) / **不一致**(字段值不同) / **多余**(目标多行)
3. 点「一键同步差异」→ 自动补 INSERT + 修 UPDATE, 每条修复结果可见
4. ⚠️ **多余行只报告不删**(删除是破坏性操作, 系统不会替你做决定), 确认要删的话到「数据中心」手工处理
5. 提示: `update_time` 这类数据库自动维护的列请加进「忽略字段」, 否则满屏假差异

### 场景 6: 不会写 SQL 也能查数 / 改数

> 想看看表里有什么数据, 或者手工修两行

「**数据中心**」页: 选数据源 → 选表
- **数据**页签: 像 Excel 一样翻页看, 有主键的表可双击编辑、可加行删行
- **表结构**页签: 看字段、加字段、改字段、管索引

### 场景 7: 会 SQL 的人的效率工具

> 临时跑个统计、排查问题

「**SQL 工作台」**: 选数据源 → 写 SQL → 执行
- `Ctrl+空格` 唤起智能补全, 输入 `.` 直出该表字段
- 右侧表结构助手点列名即插入编辑器
- `EXPLAIN` 高危项(全表扫描/filesort)自动标红提醒
- 常用 SQL 可收藏; 所有执行记录在「SQL 执行日志」留痕
- 单条语句 30 秒超时保护, 不会卡死页面

---

## 六、新手名词表

| 名词 | 人话解释 |
|------|---------|
| 全量 (FULL) | 整张表从头到尾搬一遍 |
| 增量 (INCR) | 只搬"新发生的变化"(谁插了行、谁改了值、谁删了数据) |
| binlog | MySQL 自带的"操作流水账", 增量同步靠读它, 对源库零侵入 |
| Canal | 阿里开源的 binlog 读取服务, 增量同步的前提, 需单独部署 |
| 断点续传 | 任务中断后从**上次搬到的位置**继续, 不用重头再来 |
| 幂等 | 同一批数据搬两次结果也一样, 不会搬出重复行 |
| 批次 (batch_size) | 一次搬多少行。5000 = 每次搬 5000 行歇一下 |
| 分片 (shard_count) | 把大表切成 N 块同时搬, 大表提速用 |
| CRON 表达式 | 描述"什么时候执行"的一串代码, 如 `0 0 2 * * ?` = 每天 2 点; 用页面上的生成器点选就行, 不用背 |
| 事件令牌 (token) | 事件触发 URL 里的一串随机密码, 谁拿到谁能触发任务, 保密 |
| 覆盖数据 | 每次启动先清空目标表再搬, 勾错会**丢目标库数据** |
| 数据校验 | 逐行对账, 找出两边哪行哪列不一致 |
| 一键修复 | 把缺失/不一致的行自动补齐改对(不删多余行) |

---

## 七、新手 FAQ

**Q1: 任务启动后一直没动静?**
A: 去「任务大盘」看是否在跑、速率多少; 速率 0 的话去「同步日志」按"含异常信息"筛。最常见是数据源连不上。

**Q2: 全量和增量选哪个?**
A: 第一次搬数据 → **全量**; 搬完之后要持续保持一致 → 全量跑完后**再建增量任务**。两者可以并存。

**Q3: 增量任务报 "Canal 连接失败"?**
A: 检查 Canal Server 是否启动、Host/Port/Destination 是否填对。Destination 要和 Canal 配置里的 instance 名一致。

**Q4: 目标表字段比源表多/名字不一样怎么办?**
A: 任务编辑页配「字段映射」, 拖拽连线源列 → 目标列; 多出来的列(如目标库自增列)加进「忽略字段」。

**Q5: 同步很慢怎么提速?**
A: ① 全量+ID 模式把分片数调到 4~8; ② 批次大小保持 1000~10000; ③ 看「任务大盘」瓶颈库, 是源库读得慢还是目标库写得慢。

**Q6: 我想重头再搬一遍?**
A: 任务列表 →「重置进度」清断点; 若目标表有旧数据, 勾「覆盖数据」(会先清空目标表)或手工清表。

**Q7: 任务能不能同时跑两个?**
A: 不同任务可以并行; **同一个任务**同时只允许一个在跑(定时触发撞上运行中会自动跳过本次)。

**Q8: 数据校验提示几千条差异, 正常吗?**
A: 先看差异类型: 全是 `update_time` 之类自动维护列 → 加忽略字段重跑; 大量 MISSING → 说明同步漏了或目标表被清过, 先点一键修复再查原因。

**Q9: 谁动过我的任务配置?**
A: 「审计日志」页(admin 可见), 能看到谁、什么时候、把哪个字段从什么值改成了什么值。

**Q10: 向导里没有我想配的参数(比如 DML 过滤、邮件告警)?**
A: 向导第 3 步底部展开「高级设置」就有批次/分片/覆盖/告警; DML 过滤在选「增量同步」后直接出现。
更早还能用右上角的「**高级表单**」按钮新建(就是原来的完整表单), 或建好后点任务行的「编辑任务」改全部参数。

**Q11: 忘记密码 / 加新同事账号?**
A: 找系统管理员在「系统管理 → 用户管理」里处理。

---

## 八、下一步阅读

| 想了解 | 看哪 |
|--------|------|
| 部署安装 / 5 分钟跑起来 | [QUICKSTART.md](QUICKSTART.md) |
| 某个功能的完整参数说明、性能压测数据 | [README.md](README.md) |
| 开源边界 / 授权模式 | [LICENSING.md](LICENSING.md) |
| 接口调试 | 后端 Swagger: `http://后端地址/swagger-ui/index.html` |
