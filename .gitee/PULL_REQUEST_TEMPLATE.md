## 变更说明

<!-- 一句话说清这个 PR 做了什么、为什么做 -->

## 合并流向自检

本仓库流向为 `feature/* → dev → test → master`，请确认本次 PR 的目标分支符合流向：

- [ ] `feature/*` → `dev`（日常功能开发）
- [ ] `dev` → `test`（集成自测通过，进入测试环境）
- [ ] `test` → `master`（测试环境验证通过，准备发布）
- [ ] `hotfix/*` → `master`（线上紧急修复，**已回合并 `dev`**）

## 变更类型

- [ ] 新功能 (feature)
- [ ] 缺陷修复 (fix)
- [ ] 重构 / 性能优化 (refactor / perf)
- [ ] 文档 (docs)
- [ ] 构建 / 依赖 / 配置 (chore)

## 影响范围

| 项 | 内容 |
| --- | --- |
| 影响模块 | <!-- 如: 增量同步引擎 / 数据校验 / 审计日志 / 前端任务页 --> |
| 是否涉及数据库变更 | <!-- 否 / 是（附 sql/upgrade_*.sql 文件名） --> |
| 是否涉及配置变更 | <!-- 否 / 是（说明新增的配置项与默认值） --> |
| 是否需要重启生效 | <!-- 是 / 否 --> |

## 自测情况

<!-- 说明怎么验证的: 执行的接口 / 页面操作步骤 / 观察到的结果 -->

- [ ] 后端 `./mvnw -DskipTests compile` 通过
- [ ] 涉及接口已用真实请求验证（贴关键返回或日志）
- [ ] 涉及前端页面已在浏览器验证
- [ ] 涉及数据库变更时，升级脚本已在本地库执行通过
- [ ] 已确认不影响存量任务（老配置留空 = 老行为）

## 其他说明 / 截图

<!-- 有界面改动的贴前后对比截图；无则删除本节 -->
